<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.a41">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._41"
        tools:targetApi="31">

        <!-- Launch Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- 👈 this is the fix -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Add your AddeditaskActivity here -->
        <activity android:name=".AddeditaskActivity" />

        <!-- Optional: TaskDetailActivity -->
        <activity android:name=".TaskDetailActivity" />

    </application>
</manifest>
